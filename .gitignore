__pycache__/
*.pyc
.venv/

# Generierte Ausgaben. Die CSVs und run_*.json sind bewusst versioniert: aus
# ihnen wird die komplette Report-Historie der Pages-Seite neu gebaut, und
# git log data/ ist die Rebalancing-Historie. PDF und HTML-Report sind
# Wegwerfdaten und liegen als Actions-Artefakt.
/data/factsheets/
/data/report_*.html
run.log
